# 🔍 تقرير الفحص الشامل لمنصة لوما + قائمة التعديلات المطلوبة

> **تاريخ الفحص:** 2026-09-06 · **الفرع:** `claude/system-testing-modifications-l33316`
> **نطاق الفحص:** 27 ملف HTML (16 صفحة فعلية + 10 تحويلات + صفحة 404) · 29 ملف JS · 58 عنصر قائمة · 5 لوحات تحكم · كامل رحلات العميلة والخبيرة والصالون والأدمن.

---

## أولاً · منهجية الفحص والأدلة

| ما نُفِّذ | الأداة | النتيجة |
|---|---|---|
| سويت الاختبارات القائمة (62 اختباراً) | Playwright | ✅ **62/62 ناجحة** (3.5 دقيقة) |
| زحف آلي على كل صفحة وكل عنصر قائمة | Playwright + سكربت زحف | 16 صفحة × كل عناصر قوائمها — **صفر خطأ JS** |
| فحص المعالجات المعطوبة (`onclick` يشير لدالة غير موجودة) | تقييم في نطاق الصفحة العام | **صفر معالج معطوب** |
| فحص الجوال (390×844) | قياس تخطيط فعلي + لقطات | **3 لوحات مكسورة** |
| تغطية الترجمة الإنجليزية | عدّ العُقد النصية العربية المتبقية بعد التبديل | **46٪–91٪ غير مترجَم** |
| سعة التخزين عند رفع الصور | محاكاة رفع متتابع | **يتوقف صامتاً عند ~21 صورة** |
| التواريخ والمناطق الزمنية | تثبيت ساعة المتصفح على 01:30 بتوقيت الرياض | **يُخزَّن اليوم السابق** |
| الوصولية | فحص التركيز/الأدوار/التسميات | **95 عنصر غير قابل للوصول بالكيبورد** |
| مصادر البيانات لكل شاشة | تحليل ثابت لكل `SCREENS.*` | **19 شاشة ببيانات وهمية** |

> **ملاحظة تشغيلية:** فشل السويت محلياً لأول مرة بسبب عدم تطابق نسخة متصفح Playwright مع البيئة — لا علاقة له بالكود. تعمل كاملةً عبر `PW_CHROMIUM=/opt/pw-browsers/chromium npx playwright test`، وتعمل في GitHub Actions بلا تعديل.

---

## ثانياً · الخلاصة في سطر واحد

> **البنية سليمة والواجهة متقنة، لكن نصف النظام «قشرة عرض»:** لا أخطاء برمجية ولا أزرار مكسورة تقنياً، إلا أن **24 شاشة من 46** تعرض بيانات ثابتة مكتوبة في الكود، و**6 أزرار رئيسية بلا أي معالج**، و**لوحات التحكم غير قابلة للاستخدام على الجوال**، ورحلة الاشتراك التجاري **غير موجودة أصلاً**.

---

## ثالثاً · جرد القوائم الكامل في النظام

### 1) لوحة الصالون — `salon.html` · القائمة الجانبية (14 عنصر)

| # | عنصر القائمة | مصدر البيانات | الحالة | التعديل المطلوب |
|---|---|---|---|---|
| 1 | نظرة عامة | حي (حجوزات/طاقم) | ✅ يعمل | — |
| 2 | لوحة الحجوزات | حي | ✅ يعمل | زرّان بلا `onclick` داخل الشاشة |
| 3 | الطاقم والموارد البشرية | حي | ✅ يعمل | — |
| 4 | متجري الإلكتروني | حي (12 تبويب فرعي) | ✅ يعمل | أفضل قسم في النظام |
| 5 | العملاء | حي | ✅ يعمل | زر واحد بلا `onclick` |
| 6 | الخدمات | حي | ✅ يعمل | — |
| 7 | المخزون | حي (`window.INVENTORY`) | ✅ يعمل | — |
| 8 | **الاشتراكات** | **ثابت** (`PK[]`) | 🔴 **قشرة** | ربطها بتخزين حقيقي + تفعيل «+ باقة اشتراك» |
| 9 | **الفواتير البيعية** | **ثابت** (`INV[]`) | 🔴 **قشرة** | تقرأ `INV` مكتوبة بالكود بينما المدفوعات الحقيقية في `luma_paid` — **فواتير الشاشة لا علاقة لها بفواتير النظام** |
| 10 | **التسويق** | **ثابت** (`CAMP[]`) | 🟡 جزئي | الولاء حي، الحملات وهمية + «+ حملة جديدة» بلا معالج |
| 11 | **المالية** | **ثابت** (`BYSTAFF`/`TX`) | 🔴 **قشرة** | أرقام مالية وهمية بينما المدفوعات الفعلية متاحة |
| 12 | **الفروع** | **ثابت** (`B[]`) | 🟡 جزئي | التبديل بين الفرعين يعمل فعلياً، لكن «+ إضافة فرع» بلا معالج والفرعان مكتوبان بالكود |
| 13 | التقارير | حي (`reportData()`) | ✅ يعمل | — |
| 14 | **الإعدادات** | **ثابت بالكامل** | 🔴 **قشرة** | 4 مفاتيح تبديل بلا منطق + ساعات عمل للعرض فقط + 4 حقول بلا `id` ولا حفظ + **زر «حفظ الإعدادات» بلا `onclick` إطلاقاً** |

**القائمة الفرعية «متجري الإلكتروني» (12 تبويب):** عام · الزيارات · الخدمات · المنتجات · الحجوزات · الفريق · الهوية · تصميم المتجر · الواجهة · البنرات · الصفحات · التصنيفات — ✅ **كلها تعمل ببيانات حية** (207 زر و125 حقل مربوطة).

**تبويبات الموارد البشرية (4):** الموظفات · الطلبات · جدول الدوام · الرواتب والعمولات — ✅ كلها حية.

**مرشّح حجوزات المتجر (3):** القادمة · المكتملة · الملغاة — 🟡 الحالة مكتوبة يدوياً لا مشتقّة من التاريخ (تفصيل في P0-4).

---

### 2) لوحة الخبيرة — `expert.html` · 5 مجموعات / 13 عنصر

| المجموعة | العناصر | الحالة |
|---|---|---|
| الرئيسية | نظرة عامة | 🔴 بيانات ثابتة |
| العمل اليومي | الحجوزات · العملاء · التقييمات | 🔴 بيانات ثابتة (`dash-daily.js` بلا أي تخزين) |
| حضورك على لوما | ملفي الشخصي · موقعي التعريفي · معرض الأعمال · خدماتي | 🔴 بيانات ثابتة (`dash-presence.js` / `dash-site.js`) |
| النمو والمال | الأرباح · التحليلات · تسويق بالعمولة | 🟡 **الوحيدة الحية جزئياً** (`dash-growth.js`) |
| متقدّم | أدوات احترافية · الاعتماد والمزامنة | 🔴 بيانات ثابتة |

> **الخلاصة:** 1 من 5 مجموعات فقط تقرأ بيانات حقيقية. الربط الحي الوحيد في القشرة هو **جرس الإشعارات**.

---

### 3) لوحة الأدمن — `admin.html` · 10 عناصر

نظرة عامة · التحليلات · مزوّدو الخدمة · الحجوزات · المدفوعات · العمولات · المسوّقون · الباقات · التقارير · الإعدادات

🔴 **10 من 10 قشرة عرض.** الملفات الأربعة (`admin-overview.js`, `admin-screens1/2/3.js`) فيها **27 مصفوفة ثابتة و صفر استدعاء لـ `LumaStore` أو `localStorage`**. الربط الحي الوحيد: جرس الإشعارات.
**التعديل:** لوحة الأدمن هي مرآة المنصة — يجب أن تقرأ الحجوزات والمدفوعات والصوالين من نفس المفاتيح التي تكتبها اللوحات الأخرى.

---

### 4) تطبيق العميلة — `client.html` · 5 تبويبات

الرئيسية · استكشفي · حجوزاتي · المفضلة · حسابي — ✅ **كلها تعمل**، وتبويب «استكشفي» أغناها (فلاتر: 3 أنواع + 8 تخصصات + 5 مدن).

---

### 5) بوابة الموظفة — `staff-portal.html` · 4 تبويبات

جدولي · أرباحي · طلباتي · حضوري — ✅ تعمل (مخفية حتى تسجيل الدخول باسم الموظفة، لذلك لا تظهر للزحف الآلي قبل الدخول).

---

### 6) قوائم المحرّرات والفلاتر

| القائمة | العناصر | الحالة |
|---|---|---|
| تخطيطات موقع الخبيرة | كلاسيك · مينيمال · بطاقات | ✅ |
| أغلفة موقع الخبيرة | متدرّج · لون ثابت · نقشة | ✅ |
| خطوط موقع الخبيرة | كلاسيكي · عصري | ✅ |
| أقسام موقع الخبيرة | 6 أقسام (نبذة/خدمات/معرض/تقييمات/إهداء/أسئلة) | ✅ |
| شكل بطاقات المتجر | ناعمة · حادة · بإطار | ✅ |
| شكل أزرار المتجر | حسب الاستدارة · كبسولة · مستدير · مربع | ✅ |
| عرض المنتجات/الخدمات | شبكة/قائمة · قائمة/بطاقات | ✅ |
| شكل قائمة المتجر | بسيطة · حبوب · شريط سفلي | ✅ |
| صفحات المتجر الجاهزة | 8 صفحات (من نحن … سياسة الاسترجاع) | ✅ |

---

## رابعاً · قائمة التعديلات المطلوبة (مرتّبة بالأولوية)

### 🔴 P0 — حرجة (تمنع الاستخدام الحقيقي أو تسبب ضرراً تجارياً)

**P0-1 · لوحات التحكم غير صالحة على الجوال**
الشريط الجانبي مثبّت على `262px` بلا أي `@media` — فينضغط المحتوى الرئيسي إلى **128px فقط** على شاشة 390px، ويفيض المستند أفقياً.
- `salon.html` → تجاوز **423px** · `expert.html` → **260px** · `admin.html` → **190px**
- مالكة الصالون تدير عملها من جوالها، وهذه أهم شاشاتها.
- **التعديل:** نقطة كسر تحوّل الشريط الجانبي إلى قائمة منسدلة/سفلية تحت 900px (كما في `client.html` و`staff-portal.html` — النمط موجود بالفعل في النظام).
- **الملفات:** `salon.html` (`.app{grid-template-columns:var(--sidebar-w) 1fr}`)، `expert.html`، `admin.html`.

**P0-2 · تعارض الحجوزات: الخانات بالساعة تتجاهل مدة الخدمة**
`booking.html:272` يولّد خانة لكل ساعة كاملة ويحجز خانة واحدة فقط مهما كانت الخدمة.
- «مكياج عروس» 3 ساعات يحجز الساعة 5:00 فقط → **الساعة 6:00 تبقى متاحة لعميلة أخرى لدى نفس الموظفة**.
- **التعديل:** توليد الخانات بخطوة 15/30 دقيقة وحجب كل الخانات التي تغطيها مدة الخدمة.

**P0-3 · الحجز متاح 3 أيام فقط**
`booking.html:263` → `Array.from({length:3})`. لا يمكن حجز عرس أو مناسبة بعد أسبوع.
- **التعديل:** نافذة 30 يوماً مع تنقّل بين الأسابيع.

**P0-4 · التواريخ: توقيت UTC + تقويم هجري للعميلة وميلادي للصالون**
- `toISOString().slice(0,10)` في **13 موضعاً** يعطي تاريخ UTC لا التاريخ المحلي. **مُثبَت عملياً:** الساعة 01:30 بتوقيت الرياض يوم 10 سبتمبر → الحجز يُخزَّن ليوم **9 سبتمبر**.
- `toLocaleDateString('ar-SA')` يعرض **الهجري** افتراضياً: العميلة ترى «٢٨ ربيع الأول» بينما لوحة الصالون والفاتورة تعرضان «2026-09-10».
- كذلك حالة الحجز (`up`/`done`) مكتوبة يدوياً لا مشتقّة من التاريخ → حجوزات 2026-08-12 لا تزال تظهر «**قادم**» اليوم 2026-09-06.
- **التعديل:** دالة `localISO(d)` موحّدة + `ar-SA-u-ca-gregory` (أو عرض التقويمين) + اشتقاق الحالة من التاريخ.
- **الملفات:** `booking.html`, `salon-app.js`, `salon-hr.js`, `salon-page.js`, `staff-portal.html`, `experience.html`.

**P0-5 · تعارض الأسعار: بلا ضريبة للعميلة و+15٪ عند الدفع**
`booking.html` و`store.html` و`client.html` و`experience.html` لا تذكر الضريبة إطلاقاً (0 إشارة)، بينما `salon-app.js:566` يحسب `price*1.15` عند الدفع.
- العميلة تحجز بـ450 ر.س وتُطالَب بـ517.50 ر.س. في السعودية الأسعار المعروضة للمستهلك يجب أن تكون **شاملة الضريبة**.
- **التعديل:** توحيد الأسعار كشاملة للضريبة، وعرض «شامل ضريبة القيمة المضافة» في صفحة الحجز، وتفكيك الضريبة في الفاتورة فقط.

**P0-6 · رمز ZATCA يحمل بيانات غير صحيحة**
في `salon-app.js`:
1. `zatcaQR(c.title, c.vatno || '000000000000000', p)` → **رقم ضريبي وهمي** يُطبع داخل QR إذا لم تُدخله المالكة، على فاتورة مكتوب عليها «متوافق مع فاتورة (ZATCA)».
2. `tlv(3, p.date+'T12:00:00Z')` → **وقت ثابت 12:00 UTC** بدل وقت الإصدار الفعلي.
3. البقشيش داخل `total` المُرمَّز في الحقل 4 → **الإجمالي لا يساوي المبلغ + الضريبة** عند وجود بقشيش.
4. لا تحقق من صيغة الرقم الضريبي (15 رقماً يبدأ وينتهي بـ3).
- **التعديل:** منع توليد QR بلا رقم ضريبي صحيح، وختم الوقت الفعلي، وإخراج البقشيش من الوعاء الخاضع للضريبة.

**P0-7 · رفع الصور يتوقف صامتاً عند امتلاء التخزين**
كل الصور تُحفظ base64 داخل `localStorage`. **مُثبَت عملياً:** بعد **21 صورة (~4.9MB)** تفشل الكتابة — **بلا أي رسالة، ولا خطأ، ولا تنبيه**، والصورة تظهر على الشاشة كأنها حُفظت ثم تختفي بعد التحديث.
`luma-store.js` يكتفي بـ`console.warn`، و`image-slot.js` يبتلع الخطأ بـ`catch(e){}`.
- **التعديل العاجل:** رسالة صريحة عند الفشل + عدّاد مساحة + منع فقدان صامت. (الحل الجذري: رفع الصور لتخزين حقيقي — المرحلة 3 في خارطة الطريق.)

**P0-8 · لا توجد مصادقة فعلية**
- كلمة المرور الموحّدة `123654` **مكتوبة في الكود ومعروضة على شاشة الدخول** (`login.html:204`).
- `salon-core.js:3` و`expert.html:172` **يمنحان الدور لأنفسهما** بمجرد فتح الصفحة: `localStorage.setItem('luma_role','salon')`.
- أي زائر يفتح `salon.html` مباشرة يصبح مالكة صالون، ويحصل على صلاحية `LUMA_OWNER` التي تحكم تحرير المتجر ورفع صور الصالون.
- **التعديل:** ضمن المرحلة 3 (Supabase Auth) في خارطة الطريق — لكن **حتى ذلك الحين** يجب ألّا يمنح فتح الصفحة الدورَ تلقائياً.

**P0-9 · وثائق داخلية منشورة للعامة**
- `pricing.html` هو **وثيقة اعتماد للشريك التشغيلي** (قسم «§ 09 الاعتماد»، «ملاحظة الشريك التشغيلي»، **خانة توقيع وتاريخ**) — ومع ذلك زر الصفحة الرئيسية «**ابدئي مجاناً 14 يوماً**» (`index.html:266`) يوجّه إليها مباشرة.
- `profile.html` **عرض مستثمرين** يحوي التوقعات المالية لـ12 شهراً (الإيرادات، نقطة التعادل، خطة التوسع) — منشور بلا حماية ولا `robots.txt`.
- **التعديل:** فصل صفحة أسعار تسويقية عامة عن وثيقة الاعتماد، ومنع فهرسة الوثائق الداخلية.

**P0-10 · لا توجد رحلة اشتراك إطلاقاً**
`pricing.html` تحوي **رابطاً واحداً فقط** في الصفحة كلها (رابط قفز داخلي) — **لا زر اشتراك، ولا بدء تجربة، ولا دفع**. وقمع التحويل ينتهي عند وثيقة الاعتماد.
كذلك **حدود الباقات غير مطبَّقة في أي مكان**: الصفحة تُعلن «40 حجزاً/شهر · 20 صورة · 150 عميلة · مستخدم واحد» لباقة سَناء، لكن لا يوجد مفتاح `luma_plan` في النظام أصلاً ولا أي فحص للحدود.
- **التعديل:** تخزين باقة المشتركة + فرض الحدود + مسار اشتراك فعلي.

---

### 🟠 P1 — عالية (تُضعف المنتج بشكل ملموس)

**P1-1 · الترجمة الإنجليزية ناقصة بشدة**
عند التبديل للإنجليزية يتحوّل الاتجاه إلى `ltr` بينما يبقى أغلب النص عربياً — النتيجة صفحة مشوّهة:

| الصفحة | نص عربي متبقٍ | الصفحة | نص عربي متبقٍ |
|---|---|---|---|
| بوابة الموظفة | **91٪** | المتجر | 60٪ |
| لوحة الأدمن | **80٪** | تطبيق العميلة | 54٪ |
| لوحة الخبيرة | **79٪** | صفحة الحجز | 48٪ |
| دليل الصوالين | 72٪ | الباقات | 46٪ |
| صفحة الخبيرة | 69٪ | الدخول | 16٪ |
| الملف التعريفي | 66٪ | الرئيسية | 9٪ ✅ |
| لوحة الصالون | 64٪ | | |

السبب بنيوي: قاموس مطابقة نص-لنص (`luma-i18n.js`) لا يغطي النصوص المولّدة ديناميكياً.
- **التعديل:** إمّا مفاتيح ترجمة (`data-i18n`) بدل مطابقة النصوص، أو **قصر زر اللغة على الصفحات المغطاة فعلاً** بدل عرضه في كل مكان.

**P1-2 · بيانات تجريبية بتواريخ منتهية**
حجوزات وطلبات إجازة بتواريخ 2026-06 → 2026-08 مكتوبة في `salon-page.js` و`salon-hr.js` تُعرض اليوم على أنها «قادمة» و«بانتظار الاعتماد».
- **التعديل:** توليد البيانات التجريبية نسبةً لتاريخ اليوم (`اليوم + 3` بدل `2026-08-12`).

**P1-3 · صفر تهيئة لمحركات البحث ومشاركة الروابط**
لا `description` ولا `og:` ولا `twitter:` ولا `canonical` في **أي** صفحة، ولا `robots.txt` ولا `sitemap.xml`.
- الأثر المباشر: عندما تشارك الخبيرة رابط متجرها على إنستغرام أو واتساب **لا تظهر أي صورة أو وصف** — وهذه هي آلية النمو الأساسية للمنصة.
- **التعديل:** بطاقات OG لكل صفحة عامة + صورة مشاركة لكل متجر + `robots.txt` يمنع فهرسة الوثائق الداخلية.

**P1-4 · الوصولية**
- **95 عنصراً تفاعلياً** من نوع `<div onclick>` بلا `tabindex` ولا `role` → غير قابلة للوصول بالكيبورد أو قارئ الشاشة (الصالون 28 · المتجر 48 · الخبيرة 19).
- **82 حقل إدخال من 125** في محرر المتجر بلا `label` أو `aria-label` أو `placeholder`.
- لا رابط «تخطّي إلى المحتوى»، و`client.html` بلا `<h1>` ولا معالم (`landmarks`).
- 19 هدف لمس أصغر من 32px في `booking.html` على الجوال.

**P1-5 · لا تحقق من صحة المدخلات**
`booking.html` بلا `required` ولا `pattern` ولا `type="tel"` — يمكن إتمام حجز باسم فارغ أو جوال غير صالح. و«نسيتِ كلمة المرور؟» مجرد `alert()`.

---

### 🟡 P2 — متوسطة (جودة وأداء)

| # | الملاحظة | التفصيل |
|---|---|---|
| P2-1 | **404KB جافاسكربت حاجب في `salon.html`** | 14 سكربت بلا `defer`/`async`، منها `qrcode.js` (55KB) يُحمَّل في كل زيارة رغم أنه لا يُستخدم إلا عند طباعة فاتورة |
| P2-2 | **`sw.js` يخدم HTML من الكاش أولاً** | المستخدمة ترى النسخة القديمة بعد كل نشر حتى الزيارة التالية · `luma-v1` لا يتغيّر آلياً |
| P2-3 | **الاعتماد الكلي على خطوط Google** | 3 طلبات خارجية لكل صفحة بلا بديل محلي — إذا حُجبت أو تأخرت تتغيّر هوية الصفحة بالكامل |
| P2-4 | **جدول المقارنة يفيض على الجوال** | `#ctable` بعرض 620px داخل شاشة 390px بلا حاوية تمرير · `profile.html` صفحات A4 ثابتة (تجاوز 404px) |
| P2-5 | **9 مسارات رفع صور بإعدادات مختلفة** | الحد الأقصى يتراوح 480–1600px والجودة 0.72–0.86، وأحدها PNG بلا ضغط — لا معيار موحّد |
| P2-6 | **معرّف `id` مكرر** | `aag` مرتان في `admin.html` |
| P2-7 | **كود ميت** | `S.site` معرّفة مرتين (`dash-presence.js` ثم `dash-site.js` يستبدلها) — النسخة الأولى لا تُستخدم أبداً |
| P2-8 | **صورة بلا `alt`** في `store.html` | |
| P2-9 | **أرقام جوال وهمية مبعثرة** | `0551234567`, `05000000xx`, `05551011xx` داخل كود الإنتاج |

---

### 🔵 P3 — تحسينات مقترحة

- توحيد رسائل الخطأ والفراغ (Empty States) — عدة شاشات تعرض جدولاً فارغاً بلا رسالة.
- تصدير البيانات (CSV/PDF) من شاشات التقارير والفواتير.
- سجل تدقيق (من غيّر ماذا ومتى) قبل تعدد المستخدمين.
- ضغط/تصغير ملفات JS عند النشر.
- إضافة الاختبارات الناقصة: تعارض المواعيد، حدود الباقات، التخطيط على الجوال، تغطية الترجمة.

---

## خامساً · ما هو سليم ولا يحتاج تعديل ✅

- **صفر خطأ JavaScript** في كل الصفحات والشاشات.
- **صفر معالج `onclick` معطوب** (لا دوال مفقودة).
- **62/62 اختبار Playwright ناجح.**
- **قسم «متجري الإلكتروني»** — 12 تبويباً حياً مع 207 زر و125 حقل مربوطة فعلياً: أنضج جزء في المنصة.
- **الموارد البشرية** — دورة كاملة تعمل: طلب من البوابة → سلسلة اعتماد → انعكاس على الدوام والرواتب.
- **عزل بيانات الفروع** (`salon-branch.js`) — تصميم نظيف ومحكم.
- **طبقة `LumaStore`** — إصدار مخطط وهجرات واشتراكات ومزامنة بين التبويبات: أساس جيد جداً للانتقال إلى Supabase.
- **الهوية البصرية والتناسق** — ممتازة على سطح المكتب.

---

## سادساً · علاقة التقرير بخارطة الطريق `ROADMAP.md`

- **مغطّى مسبقاً** (المرحلة 3 و4): المصادقة الحقيقية · قاعدة البيانات · واتساب · بوابة الدفع. ملاحظات P0-7 وP0-8 تُغلق نهائياً بإنجاز المرحلة 3.
- **غير مذكور في خارطة الطريق ويحتاج إضافة:** تخطيط الجوال للوحات (P0-1) · تعارض الحجوزات ومدة الخدمة (P0-2) · نافذة الحجز (P0-3) · التواريخ والتقويم (P0-4) · اتساق الضريبة (P0-5) · دقة رمز ZATCA (P0-6) · قمع الاشتراك وحدود الباقات (P0-10) · تغطية الترجمة (P1-1) · تهيئة محركات البحث (P1-3) · الوصولية (P1-4).
- **يستحق إعادة تقييم:** المرحلة 4 تعتبر بند ZATCA مكتملاً ✅، لكن الفحص يُظهر أن الرمز يحمل رقماً ضريبياً وهمياً ووقتاً ثابتاً — **يحتاج إعادة فتح**.

---

## سابعاً · ترتيب تنفيذ مقترح

| الموجة | البنود | الأثر |
|---|---|---|
| **1 — أيام** | P0-1 (الجوال) · P0-4 (التواريخ) · P0-2/P0-3 (منطق الحجز) · P1-2 (البيانات المنتهية) | تجعل المنتج قابلاً للاستخدام الفعلي على الجوال وتمنع تعارض المواعيد |
| **2 — أسبوع** | P0-5/P0-6 (الضريبة وZATCA) · P0-7 (تنبيه التخزين) · P0-9 (فصل الوثائق الداخلية) · P1-3 (OG وSEO) | تغلق المخاطر التجارية والامتثالية |
| **3 — أسبوعان** | تحويل الشاشات الـ19 القشرية إلى بيانات حية (الأدمن أولاً ثم الخبيرة ثم إعدادات الصالون) · P0-10 (الاشتراك والحدود) | تحوّل العرض إلى منتج |
| **4 — مستمر** | P1-1 (الترجمة) · P1-4 (الوصولية) · P2 كاملة | جودة ونضج |

---

*أُعدّ هذا التقرير بفحص آلي وتحليل يدوي كاملين للنظام — كل رقم فيه مُثبَت بتنفيذ فعلي لا بتقدير.*
